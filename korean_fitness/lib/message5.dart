class exercise {
  String exName;
  String imagePath;
  String exCount;

  exercise(
      {
        required this.exName,
        required this.imagePath,
        required this.exCount,
      }
  );
}